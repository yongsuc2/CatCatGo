# 프로젝트 구조

## 작성일
2026-03-06

## Assembly Structure (컴파일 의존성)
```
CatCatGo.Shared          ← 의존성 없음 (서버 공유 DTO: Request/Response)
CatCatGo.Infrastructure  ← 의존성 없음 (SeededRandom, EventBus, JsonDataLoader)
CatCatGo.Network         ← Shared 참조 (ApiClient, TokenStore, API 래퍼)
CatCatGo.Domain          ← Infrastructure 참조 (Entities, Enums, ValueObjects, Battle, Data)
CatCatGo.Services        ← Domain + Infrastructure + Network + Shared 참조 (GameManager, ServerSyncService 등)
CatCatGo.Presentation    ← Domain + Services + Infrastructure + Network + Shared 참조 (UI, Screens)
```

## Folder Structure
```
Assets/_Project/
  Scripts/
    Shared/         - 서버 공유 DTO (Requests/, Responses/)
    Network/        - HTTP 클라이언트, 토큰 관리, API 래퍼
    Domain/         - 게임 핵심 로직 (엔티티, 배틀, 챕터 등)
    Infrastructure/ - 유틸리티 (PRNG, EventBus, JsonLoader)
    Services/       - 매니저 계층 (GameManager, ServerSyncService 등)
    Presentation/   - Unity UI (Screens, Components)
  Data/Json/        - 밸런스 데이터 원본 (편집용)
  Resources/        - Resources.Load용 복사본
  Prefabs/          - UI/캐릭터/이펙트 프리팹
  Art/              - 스프라이트, 애니메이션, UI 아트
  Audio/            - BGM, SFX
  Scenes/           - 게임 씬
```

## Data Flow
- 밸런스 수치는 `Data/Json/*.data.json`에서 관리
- Resources 폴더에 복사하여 런타임 로드 (JsonDataLoader)
